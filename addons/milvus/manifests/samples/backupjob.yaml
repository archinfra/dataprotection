apiVersion: dataprotection.archinfra.io/v1alpha1
kind: BackupJob
metadata:
  name: milvus-prod-manual
  namespace: backup-system
spec:
  sourceRef:
    name: milvus-prod
  storageRef:
    name: minio-primary
  retentionRef:
    name: keep-last-3
  notificationRefs:
    - name: ops-webhook
  reason: manual milvus backup
