apiVersion: dataprotection.archinfra.io/v1alpha1
kind: RestoreJob
metadata:
  name: milvus-prod-restore
  namespace: backup-system
spec:
  sourceRef:
    name: milvus-prod
  snapshotRef:
    name: replace-with-snapshot-name
  notificationRefs:
    - name: ops-webhook
  reason: restore milvus snapshot
