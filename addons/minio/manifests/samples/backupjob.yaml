apiVersion: dataprotection.archinfra.io/v1alpha1
kind: BackupJob
metadata:
  name: minio-source-manual
  namespace: backup-system
spec:
  sourceRef:
    name: minio-source
  storageRef:
    name: nfs-primary
  retentionRef:
    name: keep-last-3
  notificationRefs:
    - name: ops-webhook
  reason: manual minio source backup
