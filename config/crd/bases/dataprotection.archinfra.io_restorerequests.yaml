---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: restorerequests.dataprotection.archinfra.io
spec:
  group: dataprotection.archinfra.io
  names:
    kind: RestoreRequest
    listKind: RestoreRequestList
    plural: restorerequests
    singular: restorerequest
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              backupRunRef:
                description: |-
                  LocalObjectReference contains enough information to let you locate the
                  referenced object inside the same namespace.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              reason:
                type: string
              snapshot:
                type: string
              snapshotRef:
                description: |-
                  SnapshotRef is the preferred restore entrypoint because it carries both
                  the storage identity and the resolved storage path.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              sourceRef:
                description: |-
                  LocalObjectReference contains enough information to let you locate the
                  referenced object inside the same namespace.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              storagePath:
                description: StoragePath is the effective path inside the selected
                  storage backend.
                type: string
              storageRef:
                description: |-
                  StorageRef is only needed for raw restores that do not point at a
                  snapshot or a single-storage BackupRun.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              target:
                description: RestoreTargetSpec 定义恢复操作的目标实例或位置
                properties:
                  driverConfig:
                    description: DriverConfig 驱动级别的恢复配置（例如 MySQL 的恢复模式）
                    properties:
                      milvus:
                        description: Milvus Milvus 驱动配置
                        properties:
                          collections:
                            description: Collections 要备份的集合列表，格式 "database.collection"
                            items:
                              type: string
                            type: array
                          databases:
                            description: Databases 要备份的数据库列表（Milvus 2.2+ 支持多库）
                            items:
                              type: string
                            type: array
                          includeObjectStorage:
                            description: IncludeObjectStorage 是否同时备份底层的对象存储数据
                            type: boolean
                        type: object
                      minio:
                        description: MinIO MinIO 驱动配置
                        properties:
                          buckets:
                            description: Buckets 要备份的桶列表（为空表示全部）
                            items:
                              type: string
                            type: array
                          includeVersions:
                            description: IncludeVersions 是否备份对象的历史版本（需启用版本控制）
                            type: boolean
                          prefixes:
                            description: Prefixes 仅备份桶内指定前缀的对象
                            items:
                              type: string
                            type: array
                        type: object
                      mongodb:
                        description: MongoDB MongoDB 驱动配置
                        properties:
                          collections:
                            description: Collections 要备份的集合列表，格式 "database.collection"
                            items:
                              type: string
                            type: array
                          databases:
                            description: Databases 要备份的数据库列表（为空表示全部）
                            items:
                              type: string
                            type: array
                          includeUsersRoles:
                            description: IncludeUsersRoles 是否同时备份用户和角色定义
                            type: boolean
                        type: object
                      mysql:
                        description: MySQL MySQL 驱动配置
                        properties:
                          databases:
                            description: Databases 要备份的数据库列表（为空表示全部）
                            items:
                              type: string
                            type: array
                          mode:
                            description: Mode 操作模式：逻辑备份（logical）或物理备份（physical）
                            type: string
                          restoreMode:
                            description: RestoreMode 恢复模式：merge（合并）、wipe-all-user-databases（清空所有用户数据库后恢复）
                            type: string
                          tables:
                            description: Tables 要备份的表列表，格式为 "database.table"，为空表示数据库内全部表
                            items:
                              type: string
                            type: array
                        type: object
                      rabbitmq:
                        description: RabbitMQ RabbitMQ 驱动配置
                        properties:
                          includeDefinitions:
                            description: IncludeDefinitions 是否备份交换机、队列、绑定等定义
                            type: boolean
                          queues:
                            description: Queues 要备份的队列列表，格式 "vhost/queueName"
                            items:
                              type: string
                            type: array
                          vhosts:
                            description: Vhosts 要备份的虚拟主机列表（为空表示所有 vhost）
                            items:
                              type: string
                            type: array
                        type: object
                      redis:
                        description: Redis Redis 驱动配置
                        properties:
                          databases:
                            description: Databases 要备份的数据库索引列表（-1 表示所有）
                            items:
                              format: int32
                              type: integer
                            type: array
                          keyPrefix:
                            description: KeyPrefix 仅备份匹配指定前缀的键（支持通配符）
                            items:
                              type: string
                            type: array
                          mode:
                            description: Mode 模式：rdb（快照）、aof（追加日志）或混合
                            type: string
                        type: object
                    type: object
                  endpoint:
                    description: Endpoint 显式指定的连接端点（优先级高于 TargetRef）
                    properties:
                      host:
                        description: Host 服务主机名或 IP
                        type: string
                      passwordFrom:
                        description: PasswordFrom 从 Secret 中获取密码（必填）
                        properties:
                          key:
                            description: Key Secret 中存放敏感数据的字段名
                            type: string
                          name:
                            description: Name Secret 的名称（位于同一命名空间）
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      port:
                        description: Port 服务端口
                        format: int32
                        type: integer
                      scheme:
                        description: Scheme 连接协议（如 http, https, tcp）
                        type: string
                      serviceRef:
                        description: ServiceRef 可选的 Service 引用，优先级高于 Host/Port
                        properties:
                          name:
                            description: Name Service 名称
                            type: string
                          namespace:
                            description: Namespace Service 所在命名空间（默认为当前资源的命名空间）
                            type: string
                          port:
                            description: Port Service 的端口号
                            format: int32
                            type: integer
                        required:
                        - name
                        type: object
                      username:
                        description: Username 明文用户名（不推荐）
                        type: string
                      usernameFrom:
                        description: UsernameFrom 从 Secret 中获取用户名
                        properties:
                          key:
                            description: Key Secret 中存放敏感数据的字段名
                            type: string
                          name:
                            description: Name Secret 的名称（位于同一命名空间）
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    type: object
                  mode:
                    description: Mode 恢复模式：原地恢复或异机恢复
                    type: string
                  targetRef:
                    description: TargetRef 引用一个已存在的 Kubernetes 对象（如 BackupSource 或
                      Service）
                    properties:
                      apiVersion:
                        description: APIVersion 对象的 API 版本
                        type: string
                      kind:
                        description: Kind 对象的资源类型
                        type: string
                      name:
                        description: Name 对象名称
                        type: string
                      namespace:
                        description: Namespace 对象所在命名空间
                        type: string
                    required:
                    - name
                    type: object
                type: object
            required:
            - sourceRef
            type: object
          status:
            properties:
              completedAt:
                format: date-time
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              jobName:
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: ResourcePhase 定义资源当前的生命周期阶段
                type: string
              startedAt:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
