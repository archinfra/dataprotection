---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: backupruns.dataprotection.archinfra.io
spec:
  group: dataprotection.archinfra.io
  names:
    kind: BackupRun
    listKind: BackupRunList
    plural: backupruns
    shortNames:
    - br
    singular: backuprun
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.policyRef.name
      name: Policy
      type: string
    - jsonPath: .spec.sourceRef.name
      name: Source
      type: string
    - jsonPath: .status.completedAt
      name: Completed
      type: date
    - jsonPath: .spec.snapshot
      name: Snapshot
      priority: 1
      type: string
    - jsonPath: .status.message
      name: Message
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              driverConfig:
                description: DriverConfig 各数据驱动的专用配置（联合类型）
                properties:
                  milvus:
                    description: Milvus Milvus 驱动配置
                    properties:
                      collections:
                        description: Collections 要备份的集合列表，格式 "database.collection"
                        items:
                          type: string
                        type: array
                      databases:
                        description: Databases 要备份的数据库列表（Milvus 2.2+ 支持多库）
                        items:
                          type: string
                        type: array
                      includeObjectStorage:
                        description: IncludeObjectStorage 是否同时备份底层的对象存储数据
                        type: boolean
                    type: object
                  minio:
                    description: MinIO MinIO 驱动配置
                    properties:
                      buckets:
                        description: Buckets 要备份的桶列表（为空表示全部）
                        items:
                          type: string
                        type: array
                      includeVersions:
                        description: IncludeVersions 是否备份对象的历史版本（需启用版本控制）
                        type: boolean
                      prefixes:
                        description: Prefixes 仅备份桶内指定前缀的对象
                        items:
                          type: string
                        type: array
                    type: object
                  mongodb:
                    description: MongoDB MongoDB 驱动配置
                    properties:
                      collections:
                        description: Collections 要备份的集合列表，格式 "database.collection"
                        items:
                          type: string
                        type: array
                      databases:
                        description: Databases 要备份的数据库列表（为空表示全部）
                        items:
                          type: string
                        type: array
                      includeUsersRoles:
                        description: IncludeUsersRoles 是否同时备份用户和角色定义
                        type: boolean
                    type: object
                  mysql:
                    description: MySQL MySQL 驱动配置
                    properties:
                      databases:
                        description: Databases 要备份的数据库列表（为空表示全部）
                        items:
                          type: string
                        type: array
                      mode:
                        description: Mode 操作模式：逻辑备份（logical）或物理备份（physical）
                        type: string
                      restoreMode:
                        description: RestoreMode 恢复模式：merge（合并）、wipe-all-user-databases（清空所有用户数据库后恢复）
                        type: string
                      tables:
                        description: Tables 要备份的表列表，格式为 "database.table"，为空表示数据库内全部表
                        items:
                          type: string
                        type: array
                    type: object
                  rabbitmq:
                    description: RabbitMQ RabbitMQ 驱动配置
                    properties:
                      includeDefinitions:
                        description: IncludeDefinitions 是否备份交换机、队列、绑定等定义
                        type: boolean
                      queues:
                        description: Queues 要备份的队列列表，格式 "vhost/queueName"
                        items:
                          type: string
                        type: array
                      vhosts:
                        description: Vhosts 要备份的虚拟主机列表（为空表示所有 vhost）
                        items:
                          type: string
                        type: array
                    type: object
                  redis:
                    description: Redis Redis 驱动配置
                    properties:
                      databases:
                        description: Databases 要备份的数据库索引列表（-1 表示所有）
                        items:
                          format: int32
                          type: integer
                        type: array
                      keyPrefix:
                        description: KeyPrefix 仅备份匹配指定前缀的键（支持通配符）
                        items:
                          type: string
                        type: array
                      mode:
                        description: Mode 模式：rdb（快照）、aof（追加日志）或混合
                        type: string
                    type: object
                type: object
              policyRef:
                description: |-
                  PolicyRef is optional for manual runs, but when set the run inherits the
                  policy defaults and writes into the same storage path layout.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              reason:
                type: string
              snapshot:
                type: string
              sourceRef:
                description: SourceRef is always required so the run remains self-describing.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              storageRefs:
                description: |-
                  StorageRefs optionally narrows the policy storages to a subset. If empty
                  and PolicyRef is set, the run uses all storages from the policy.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
            required:
            - sourceRef
            type: object
          status:
            properties:
              completedAt:
                format: date-time
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              jobNames:
                items:
                  type: string
                type: array
              message:
                type: string
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: ResourcePhase 定义资源当前的生命周期阶段
                type: string
              startedAt:
                format: date-time
                type: string
              storages:
                items:
                  description: StorageRunStatus records one storage-specific execution
                    branch within a run.
                  properties:
                    message:
                      description: Message 人类可读的状态详情或错误信息
                      type: string
                    name:
                      description: Name 对应的 BackupStorage 名称
                      type: string
                    phase:
                      description: Phase 当前执行阶段（Pending/Running/Succeeded/Failed）
                      type: string
                    snapshot:
                      description: Snapshot 备份产出物的标识（如 S3 路径或快照 ID）
                      type: string
                    snapshotRef:
                      description: SnapshotRef 对应的 Snapshot CR 名称
                      type: string
                    storagePath:
                      description: StoragePath 记录这次执行最终写入的存储路径
                      type: string
                    updatedAt:
                      description: UpdatedAt 状态最近更新时间
                      format: date-time
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
