apiVersion: dataprotection.archinfra.io/v1alpha1
kind: BackupRun
metadata:
  name: minio-source-manual
  namespace: backup-system
spec:
  sourceRef:
    name: minio-source
  storageRefs:
    - name: nfs-primary
  reason: "manual smoke backup for minio source"
